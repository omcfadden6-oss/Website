<section class="about-preview">

    <div class="about-content">

        <p class="eyebrow">A LITTLE ABOUT ME</p>

        <h2>Still learning. Still building.</h2>

        <p class="description">
            I'm using this space to document my journey into motorsport -
            from technical projects and data analysis to the things I'm
            learning along the way.
        </p>

        <a href="/about" class="about-link">
            More about my journey <span>→</span>
        </a>

    </div>

</section>

<style>
    .about-preview {
        margin-top: 3rem;
        padding: 1rem 0;
        border-top: 1px solid var(--border);
    }

    .about-content {
    }

    .eyebrow {
        color: var(--accent);
        font-size: 0.8rem;
        letter-spacing: 0.15em;
        font-weight: 600;
        margin-bottom: 1.5rem;
    }

    h2 {
        font-size: clamp(2.2rem, 5vw, 3.5rem);
        line-height: 1;
        letter-spacing: -0.04em;
        margin: 0 0 1.5rem;
    }

    .description {
        color: var(--secondary);
        font-size: 1.1rem;
        line-height: 1.7;
        margin-bottom: 2rem;
    }

    .about-link {
        color: var(--text);
        text-decoration: none;
        border-bottom: 1px solid var(--border);
        padding-bottom: 0.3rem;
        transition: border-color 0.2s ease;
    }

    .about-link:hover {
        border-color: var(--accent);
    }

    .about-link span {
        margin-left: 0.4rem;
    }
</style>