<section class="hero">
    <p class="eyebrow">PHYSICS · MOTORSPORT · DATA</p>

    <h1>Olivia McFadden</h1>

    <p class="intro">
        Physics student interested in motorsport, vehicle dynamics
        and data-driven performance analysis.
    </p>
</section>

<style>
    .hero {
    }

    .eyebrow {
        color: var(--accent);
        font-size: 0.8rem;
        letter-spacing: 0.15em;
        font-weight: 600;
        margin-bottom: 1.5rem;
    }

    h1 {
        font-size: clamp(3.5rem, 4vw, 3rem);
        line-height: 0.95;
        letter-spacing: -0.04em;
        margin: 0 0 2rem;
    }

    .intro {
        color: var(--secondary);
        font-size: 1.25rem;
        line-height: 1.6;
        margin-bottom: 1rem;
    }

    .actions {
        display: flex;
        gap: 1rem;
    }

    .actions a {
        text-decoration: none;
        padding: 0.8rem 1.2rem;
        border: 1px solid var(--border);
    }

    .primary {
        background: var(--text);
        color: var(--background);
    }

    .secondary {
        color: var(--text);
    }

    .actions a:hover {
        border-color: var(--accent);
    }
</style>